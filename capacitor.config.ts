import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.google.fcm',
  appName: 'google-fcm',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
